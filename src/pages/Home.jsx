import { SignedIn, SignedOut, SignInButton, SignOutButton, SignUpButton, UserButton } from "@clerk/clerk-react";

const Home = () => {
  return (
    <div>

      
    
      {/* กรณี LOGIN แล้ว
      <SignedIn>
        <UserButton />
        <SignOutButton/>
      </SignedIn> */}
    </div>
    
  )
}

export default Home